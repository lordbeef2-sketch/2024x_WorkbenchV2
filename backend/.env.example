# TWC Workbench backend environment configuration
#
# Notes:
# - Lines beginning with # are comments.
# - Keep secrets out of source control in shared environments.
# - JSON-like arrays such as ALLOWED_ORIGINS must stay on one line.
# - TWC_PRESET_SERVERS must stay on one line because it is parsed as JSON.
# - TWC_PRESET_SERVERS is the authoritative preset catalog used before login.

# -----------------------------------------------------------------------------
# Application identity and runtime
# -----------------------------------------------------------------------------

# Human-readable application name used in logs and API metadata.
# Example: APP_NAME=Acme TWC Workbench
APP_NAME=TWC Workbench

# Public origin of this app as seen by the browser in deployed environments.
# Use the Caddy or reverse-proxy hostname that serves the frontend.
# Leave empty in local development to fall back to FRONTEND_ORIGIN.
# Set this to the browser-visible HTTPS origin in production so AuthServer
# callbacks and cache API links resolve correctly.
# Example: APP_ORIGIN=https://twc-workbench.company.com
APP_ORIGIN=

# Runtime environment label.
# Common values: development, test, staging, production
# Example: ENVIRONMENT=production
ENVIRONMENT=development

# Bind address for this FastAPI application.
# This is the local interface the app listens on, not the Teamwork Cloud hostname.
# Use 0.0.0.0 to listen on all interfaces, 127.0.0.1 for local-only,
# or a specific local interface IP. Do not put your public FQDN here.
# Example: HOST=127.0.0.1
HOST=0.0.0.0

# Backend port.
# Example: PORT=8080
PORT=8000

# API route prefix.
# Example: API_PREFIX=/api
API_PREFIX=/api

# Optional reverse-proxy root path when the app is mounted under a subpath.
# Leave empty for normal local development.
# Example: ROOT_PATH=/twc-workbench
ROOT_PATH=

# -----------------------------------------------------------------------------
# Frontend and CORS
# -----------------------------------------------------------------------------

# Primary browser origin used by the frontend.
# Put your public app URL/FQDN here in deployed environments.
# Example: FRONTEND_ORIGIN=https://twc-workbench.company.com
FRONTEND_ORIGIN=http://localhost:5173

# Allowed CORS origins as a JSON array on one line.
# Example: ALLOWED_ORIGINS=["https://twc-workbench.company.com","https://admin.company.com"]
ALLOWED_ORIGINS=["http://localhost:5173","http://localhost:8000"]

# -----------------------------------------------------------------------------
# Storage paths
# -----------------------------------------------------------------------------

# Base directory for SQLite data.
# Example: DATA_DIR=D:/apps/twc-workbench/data
DATA_DIR=./data

# Optional explicit database file path.
# Leave empty to use DATA_DIR/twc_workbench.sqlite3.
# Example: DATABASE_PATH=D:/apps/twc-workbench/data/twc_workbench.sqlite3
DATABASE_PATH=

# Optional explicit export directory retained for internal maintenance utilities.
# It is not exposed as a workspace export/job feature.
# Leave empty to use DATA_DIR/exports.
# Example: EXPORT_DIR=D:/apps/twc-workbench/exports
EXPORT_DIR=

# Optional 3DS / No Magic 2024x knowledge export. Point this at either the
# generated KB directory or its datasheet_chunks.jsonl file. Workbench folds
# these source-attributed chunks into each Workbench Agent knowledge push.
# Example: THREE_DS_KB_PATH=C:/sand/TWC_Data_Sheets/TWC2024x/output/nomagic_owui_kb
THREE_DS_KB_PATH=

# Safety ceiling for official 3DS KB chunks included across the complete Agent reference set.
# Set to 0 to disable the global 3DS layer without moving the source files.
THREE_DS_KB_MAX_CHUNKS=50000
# Workbench splits the complete reference into multiple processed OWUI files to avoid proxy upload limits.
THREE_DS_KB_REFERENCE_FILE_MAX_BYTES=2500000
OPENWEBUI_VERIFY_TLS=true
# Optional PEM CA bundle for an internal Open WebUI certificate chain.
OPENWEBUI_CA_BUNDLE_PATH=
OPENWEBUI_ALLOW_INSECURE_HTTP=false
# Optional JSON host allowlist, for example ["owui.example.mil"].
OPENWEBUI_ALLOWED_HOSTS=[]

# -----------------------------------------------------------------------------
# Teamwork Cloud preset catalog
# -----------------------------------------------------------------------------

# Global Teamwork Cloud preset catalog loaded at startup and exposed before login.
# This JSON array is the authoritative source for pre-login server discovery.
# Keep it on one line.
# Example: TWC_PRESET_SERVERS=[{"id":"twc-2024x","name":"TWC 2024x","base_url":"https://twc2024.company.com","version":"2024x","verify_tls":true,"ca_bundle_path":null,"enabled":true,"display_order":0}]
TWC_PRESET_SERVERS=[]

# Sign In via TWC is the primary path. It uses the TWC Authentication Server
# authorization-code flow, preserves the selected preset server, exchanges the
# returned code for an ID token, and then validates that token against
# /osmc/admin/currentUser.
#
# Client id/secret generated for this Workbench in Web Application Platform
# Settings -> OAuth clients -> OpenID Connect. Register the exact callback URI.
# Exact-name aliases are also accepted: TWC_AUTHENTICATION_CLIENT_ID,
# TWC_AUTHENTICATION_CLIENT_IDS, and TWC_AUTHENTICATION_CLIENT_SECRET.
# Example: TWC_AUTH_CLIENT_ID=twc-workbench
TWC_AUTH_CLIENT_ID=
# Example: TWC_AUTH_CLIENT_SECRET=<generated OIDC client secret>
TWC_AUTH_CLIENT_SECRET=

# Official 2024x Refresh3 OIDC supports the openid scope.
TWC_AUTH_SCOPE=openid

# Official 2024x Refresh3 OIDC discovery and endpoint defaults. Workbench reads
# the discovery JSON first and uses the endpoint URLs it returns.
TWC_OIDC_DISCOVERY_URL=
TWC_OIDC_DISCOVERY_PATH=/authentication/.well-known/oidc-configuration
TWC_OIDC_AUTHORIZE_URL=
TWC_OIDC_AUTHORIZE_PATH=/authentication/oidc/authorize
TWC_OIDC_TOKEN_URL=
TWC_OIDC_TOKEN_PATH=/authentication/api/oidc/token
TWC_OIDC_PORT=8443
TWC_OIDC_TOKEN_AUTH_METHOD=client_secret_basic
TWC_OIDC_RETURN_URL_PARAMETER=redirect_uri

# Optional per-preset AuthServer overrides keyed by TWC_PRESET_SERVERS id. Use
# this when your 2024x deployment uses a different AuthServer host, proxy path,
# client id, secret, port, scope, or return parameter name. Values omitted here
# fall back to the global TWC_AUTH_* and TWC_OIDC_* settings above.
# Keep it on one line.
# Example using names that mirror TWC AuthServer properties. If
# authentication.client.ids contains more than one value, use the one registered
# for this Workbench app.
# TWC_AUTH_SERVER_OVERRIDES={"twc-2024x":{"discovery_url":"https://twc2024.company.com:8443/authentication/.well-known/oidc-configuration","client_id":"twcworkbench-twc-2024x","client_secret":"<generated-client-secret>","scope":"openid","token_auth_method":"client_secret_basic"}}
TWC_AUTH_SERVER_OVERRIDES={}

# -----------------------------------------------------------------------------
# Session and security settings
# -----------------------------------------------------------------------------

# Secret used to encrypt server-side delegated credentials and app sessions.
# Replace this with a long random value in non-local environments.
# Example: SESSION_SECRET=8d7f2d4ddc9f43e5abf55d13a6fb7f40f21c7c9e7c6a1b4d
SESSION_SECRET=replace-with-a-32-byte-secret

# Name of the HTTP-only session cookie.
# Example: SESSION_COOKIE_NAME=twc_session_prod
SESSION_COOKIE_NAME=twc_session

# Session lifetime in minutes.
# Example: SESSION_TTL_MINUTES=60
SESSION_TTL_MINUTES=480

# Workbench snapshots each logged-in user's effective permissions at login and
# atomically replaces that snapshot on this interval. Authorization reads use
# the stored snapshot between refreshes instead of repeatedly querying TWC.
PERMISSION_SNAPSHOT_REFRESH_MINUTES=30
PERMISSION_INVENTORY_REFRESH_HOURS=6
JOB_RETENTION_DAYS=30
PERMISSION_SNAPSHOT_MAX_PARALLEL_PROBES=4
PERMISSION_REFRESH_LEASE_SECONDS=900
PERMISSION_REFRESH_WARNING_FAILURES=3
# Optional HTTPS endpoint for sanitized repeated inventory-refresh failure alerts.
PERMISSION_ALERT_WEBHOOK_URL=
PERMISSION_SNAPSHOT_STALE_WARNING_MINUTES=120

# Enable secure cookies when running behind HTTPS.
# Use true in staging/production with TLS.
# Example: SECURE_COOKIES=true
SECURE_COOKIES=false

# Header name used for CSRF protection on state-changing requests.
# Example: CSRF_HEADER_NAME=X-CSRF-Token
CSRF_HEADER_NAME=X-CSRF-Token

# Optional public callback path used by Sign In via TWC.
# Leave empty to use API_PREFIX/auth/callback.
# Set this only when your deployed app must receive the callback on a different
# browser-visible path.
# Example: TWC_AUTH_CALLBACK_PATH=/api/auth/callback
TWC_AUTH_CALLBACK_PATH=

# Optional JSON array of upstream Teamwork Cloud cookie names to forward.
# Leave empty to forward all incoming cookies except the app's own cookies.
# The normal Sign In via TWC callback completes from the AuthServer code. Cookies
# or forwarded user-scoped tokens are fallback credentials when your deployment
# bypasses that code flow.
# Example: UPSTREAM_AUTH_COOKIE_NAMES=["twc-rest-session-id","JSESSIONID"]
UPSTREAM_AUTH_COOKIE_NAMES=[]

# Optional JSON array of trusted reverse-proxy headers that identify the current user.
# These headers are only meaningful when your proxy authenticates the user before
# the app. They do not replace an AuthServer code, Teamwork Cloud session cookie,
# or forwarded user-scoped TWC token for callback completion.
# Example: UPSTREAM_USER_HEADERS=["X-Forwarded-User","Remote-User"]
UPSTREAM_USER_HEADERS=["X-Forwarded-User","Remote-User","X-MS-CLIENT-PRINCIPAL-NAME","X-Authenticated-User"]

# Optional JSON array of trusted reverse-proxy headers that carry TWC group membership.
# Example: UPSTREAM_GROUP_HEADERS=["X-Forwarded-Groups","X-Groups"]
UPSTREAM_GROUP_HEADERS=["X-Forwarded-Groups","X-Groups","X-MS-CLIENT-PRINCIPAL-GROUPS","X-Authenticated-Groups"]

# Optional JSON array of trusted reverse-proxy headers that carry TWC role membership.
# Example: UPSTREAM_ROLE_HEADERS=["X-Forwarded-Roles","X-Roles"]
UPSTREAM_ROLE_HEADERS=["X-Forwarded-Roles","X-Roles","X-MS-CLIENT-PRINCIPAL-ROLES","X-Authenticated-Roles"]

# Optional JSON array of trusted reverse-proxy headers that carry a user-scoped TWC token.
# Configure one of these when your deployment returns the browser to the callback
# without an AuthServer code or Teamwork Cloud session cookies.
# Example: UPSTREAM_ACCESS_TOKEN_HEADERS=["X-Forwarded-Access-Token","Authorization"]
UPSTREAM_ACCESS_TOKEN_HEADERS=["X-Forwarded-Access-Token","X-Access-Token","Authorization"]

# Optional JSON array or comma-separated list of workbench administrators.
# These users can access administrative Workbench settings and API Explorer when
# upstream role/group claims are unavailable or incomplete.
# Example: ADMIN_USERS=["alice","bob"]
ADMIN_USERS=[]

# Optional legacy fallback JSON array of bearer tokens allowed to post
# plugin-ingested cache snapshots and deltas into the Workbench cache-ingest
# API. The preferred path is to generate and rotate the plugin ingest token
# inside Workbench Settings, where it is stored encrypted in app storage.
# Example: CACHE_INGEST_TOKENS=["replace-with-long-random-token"]
CACHE_INGEST_TOKENS=[]

# Optional legacy fallback JSON object mapping cache-read bearer token to the
# Workbench/TWC username whose cached permissions should be applied for
# token-based cache API reads. The preferred path is to let each user create
# their own API key from Workbench Settings.
# Example: CACHE_API_TOKENS={"replace-with-read-token":"alice"}
CACHE_API_TOKENS={}

# Optional JSON object keyed by server id that forces specific project ids or
# project/branch pairs to use plugin-backed cache only. When a target is listed
# here, Workbench will not fall back to live /osmc model traversal for that
# branch. Publish a Cameo plugin snapshot first.
# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------

# Logging level.
# Common values: DEBUG, INFO, WARNING, ERROR
# Example: LOG_LEVEL=DEBUG
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Optional Redis-backed session storage
# -----------------------------------------------------------------------------

# Redis connection string for shared session storage.
# Leave empty to use in-memory sessions.
# Example: REDIS_URL=redis://localhost:6379/0
REDIS_URL=
